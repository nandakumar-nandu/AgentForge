# ==============================================================================
# AGENTFORGE FRONTEND CONFIGURATION SETTINGS
# ==============================================================================

# Target API gateway URL pointing to the Express backend server instance.
# Next.js requires the 'NEXT_PUBLIC_' prefix to expose this variable to client-side code blocks.
NEXT_PUBLIC_API_URL=http://localhost:5001

